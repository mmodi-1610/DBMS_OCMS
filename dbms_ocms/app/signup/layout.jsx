export const metadata = {
  title: 'Sign up - QuadBase',
}

export default function SignupLayout({ children }) {
  return <div className="min-h-screen flex items-center justify-center">{children}</div>
}
